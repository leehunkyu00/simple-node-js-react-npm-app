pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'node -v'
                sh 'npm -v'
                sh 'npm install' 
            }
        }
    }
    tools {
        nodejs 'node18.18.0'
    }
}